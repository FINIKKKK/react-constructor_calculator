import React from "react";

import ss from "./ListButton.module.scss";

interface ListButtonProps {
  // items: TList;
}

export const ListButton: React.FC<ListButtonProps> = ({ }) => {
  return (
    <></>
  );
};
